# Changelog

## [0.2.0] - 2026-06-11

### Added
- **版本检查**：每次启动后台比对 GitHub 最新 Release，有新版时托盘菜单与设置窗口提示「去下载」；离线静默，每 24 小时复查。
- **设置窗口**（托盘 → 设置 / 双击托盘）：开机自动启动、休息触发秒数、拉屎间隔上下限、跑步速度，改动即时生效并持久化。
- **宠物素材工坊**（托盘 → 宠物素材工坊）：每个动作的拍摄角度线条示意图（侧面行走/快跑/侧躺休息/正面端坐/炸毛/蹲姿）、拍摄机位俯视总览、当前素材状态表、一键打开素材文件夹、重新扫描并热重载（无需重启）。
- **NSIS 安装包**：`npm run dist` 产出 `DesktopPet-Setup-x.y.z.exe`；单实例运行，重复启动会打开设置窗口。

### Changed
- **素材与配置同应用本体分离**：素材移至 `%APPDATA%/desktop-pet/pets/cat/`、配置移至 `%APPDATA%/desktop-pet/settings.json`。首次启动自动播种内置素材，此后升级/重装绝不覆盖用户自定义素材与设置（旧版根目录 `config.json` 的值会在首次迁移时继承）。
- **托盘图标改用用户素材帧**（优先 stand），替换素材重扫后托盘图标同步更新；托盘菜单加入设置/素材工坊/当前版本。
- manifest 重建改用 Electron `nativeImage` 在主进程完成，运行时不再依赖 sharp；`sharp`/`@imgly/background-removal-node` 移为开发依赖，发布包瘦身。

### Fixed
- 拉屎间隔设置修改后立即按新区间重新倒计时（原本要等旧倒计时走完才生效）。
